# 27王道《计算机组成原理》高清带书签

原文件：[27王道《计算机组成原理》高清带书签.pdf](../../materials/27%E7%8E%8B%E9%81%93%E3%80%8A%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BB%84%E6%88%90%E5%8E%9F%E7%90%86%E3%80%8B%E9%AB%98%E6%B8%85%E5%B8%A6%E4%B9%A6%E7%AD%BE.pdf)

以下为机器提取，未人工校对；公式、上下标、图表、选项和答案必须对照原页。PDF 页码从 1 开始，不等于印刷页码。

## PDF 第 187 页

提取方式：ocr-unverified

175
举例 ：
jmp begin
第 4 章指令系统
# 转移到 begin 标记处执行
条件转移指令与标志位的结合 （ 2013 ）
考点旧
2 ） j “ “ 指令 。 条件转移指令 ， 根据程序状态字寄存器中的状态标志 （ 如零标志 ZF 、
符号标志 SF 等 ） 决定是否转移 。 常见指令包括 ：
j e <1abe1>
j 2 <label>
力 ne <label>
j g <1abe1>
力 ge <label>
j 上 <label>
j le <1abe1>
举例 ：
Cmp eaX, ebX
j 1e done
# 相等时转移 (jump when equal)
# 结果为零时转移 (jump when last result w a s ze ro ）
# 不相等时转移 (jump when not equa 0
# 大于时转移 （ j 叩 when greate r than ）
# 大于等于时转移 (jump when greate r than 0 r egual to)
# 小于时转移 （ j 叩 when less than ）
# 小于等于时转移 (jump when less than 0 r equal 乜 0 ）
# 若 eax<ebx ， 则转移到 done ： 否则顺序执行下一条指令
3 ） cmp/test 指令 。 cmp 指令执行减法运算但不保存结果 ， 仅根据结果设置标志位 ； test 指令
执行按位与运算但不保存结果 ， 仅更新标志位 （ 特别是 ZF ） 。 其语法如下 ：
Cmp <reg>,<reg> / test < reg> ， <reg>
Cmp <reg>,<mem> / test <reg>,
Cmp <mem> ， < reg> / test <mem> ， <reg>
Cmp <reg>,<con> / test < reg> ， <con>
cmp 和 test 指令通常与 jco ， 动肋 ” 指令配合使用 。 cmp 指令举例 ：
10 # 将处的 4 字节内容 ， 与 10 比较
cmp dword ptr [var ] ，
jne IOOP
test 指令举例 ：
〕 Z XXXX
call 指今的功能 （ 2019 冫
# 若相等则顺序执行 ： 否则转移到 loop
# 测试 eax 是否为零
# 若 eax 为零 (ZF=I) ， 则转移到
4 ） call/ret 指令 。 分别用于实现子程序调用与返回 。 其语法如下 ：
caII < Ia ℃ eI >
ret
call 指令将下一条指令的地址 （ 返回地址 ） 压入栈 ， 然后转移到 label 处执行 ； ret 指令从栈
顶弹出返回地址 ， 并转移到该地址继续执行 。 call 和 ret 是函数调用机制的核心指令 。
掌握上述指令的语法与功能 ， 有助于解答相关考题 。 建议读者在学习 c 语言程序时 ， 结合调
试工具 （ 如 GDB ） 查看其对应的汇编代码 ， 以加深对机器级指令的理解 。
3 ， 汇编指令格式
使用不同的编程工具开发程序时 ， 所用的汇编器也不同 ， 主要有 AT&T 格式和 lntel 格式两
种 （ 统考常涉及的是 lntel 格式 ） 。 它们的主要区别如下 ：
． AT&T 格式的指令名必须使用小写字母 ， lntel 格式对大小写不敏感 。
@ 操作数顺序不同 ： AT&T 格式为 “ 源 ， 目的 ” ， lntel 格式为 “ 目的 ， 源 ”
@ AT & T 格式中 ， 寄存器前加 “ ％ ” ， 立即数前加 “ $ ” ； lntel 格式中 ， 两者均无前缀 。
@ 内存寻址符号不同 ： AT&T 使用圆括号 “ 0 ” ， lntel 使用方括号 “ 目 ”
0 复杂寻址方式表示不同 ： AT&T 格式的内存操作数 “ disp(base, index, scale) ” 分别表示偏
移量 、 基址寄存器 、 变址寄存器和比例因子 ， 如 “ 8 （ ％ x ， %eax ， 2 ） ” 表示操作数地址为
R[edx] + R[eax]*2 + 8 ： 对应的 lntel 格式为 “ [edx + eax*2 + 8 ] ”

## PDF 第 188 页

提取方式：ocr-unverified

176
2027 年计算机组成原理考研复习指导
@ 操作数长度指定方式不同 ： AT&T 格式在指令助记符后加后缀 ， 表明操作数大小 ， "b ”
表示 byte （ 字节 ） 、 "w ” 表示 word （ 字 ） 或 “ 广表示 long （ 双字 ） ； lntel 格式则在内
存操作数前使用 "byteptr ”
"word ptr ” 或 "dword ptr ” 显式指定长度 。
注意
在 x86 体系结构中 ， 32 或 64 位都是由 16 位扩展来的 ， 因此艹 rd （ 字 ） 始终表示 16 位 。
表 4 2 所示为 AT&T 格式与 lntel 格式的指令对比 。 其中 ， mov 指令用于在寄存器与内
存之间或寄存器之间传送数据 ； lea 指令用于将有效地址 （ 而非内存内容 ） 加载到寄存器 。
表 4 ． 2 AT&T 格式指令和 lntel 格式指令的对比
mov
mov
mOV
1ea
AT&T 格式
$ 100 ， *eax
teax ， iebx
±eax ， { %ebx)
一 8 (*ebp)
8 (%edx, 2 〕 ，
mOV
mov
mov
mov
lntel 格式
eax, 100
eaX
[ebxl ， eax
[ebp—8), eax
movl teax, %ebx
1ea eax, (edx+eax•2+81
mov dword ptr (ebxl, eax
含义
0 一 RIe
Rleax] —Rlebxl
RIeax] 乛 M [ R [ 山刈 ]
Rßnxl—M[R[ebpl-81
RIedxl+R[eaxl*2+8—Rleaxl
32 位 RIeax]—M[R[ebxll
注 ： R 囝表示寄存器 [ 的内容 ， M [ addr ] 表示主存单元 addr 的内容 ， 乛或一表示信息传送方向 。
两种汇编格式的相互转换并不复杂 ， 历年统考真题通常采用 lntel 格式 。
4 ， 3 ， 2 选择语句的机器级表示
常见的选择结构语句有 lf-then 、 if-then-else 等 。 编译器通过条件码 （ 标志位 ） 设置指令和各
类条件转移指令来实现程序中的选择结构 。 条件码描述了最近算术或逻辑运算操作的结果属性 ，
程序通过检测这些标志位来决定是否执行条件分支 。 常用的条件码有 CF 、 ZF 、 SF 和 OF 。
常见的算术逻辑运算指令 (add, sub ， mul, and,shl ， inc, dec ， not ， sal 等 ） 在执行时会自动设
置条件码 。 此外 ， cmp 和 test 指令专门用于设置条件码 ， 它们执行相应的运算 (cmp 相当于 sub,
test 相当于 and) ， 但不保存结果 ， 仅根据运算结果更新标志位 。
之前介绍的 jco “ “ 条件转移指令 ， 就是根据条件码 ZF 、 OF 或 SF 来实现转移的 。
if-else 语句的通用形式如下 ：
if (test_expr)
then statement
else statement
//test_expr 为条件测试表达式
/ ／ 当 test_expr 为真时 ， 执行 then statement 语句
/ / 当 test_expr 为假时 ， 执行 else statement 语句
在 C 语言中 ， test_expr 是一个整数表达式 ， 其值为 0 时表示 “ 假 ” ， 非 0 （ 包括负数 ） 时表示 “ 真
两个分支语句 (then statement 或 else statement ） 中只会执行其中一个 。
这种通用形式可以被翻译成如下等价的 goto 语句形式 ：
t=te St expr ；
if 0 t)
goto false;
then statement
gOtO done ；
false ：
01s0 statement
done ：
/ / 暂存测试表达式的结果
/ ／ 若条件为假 (t=0)
/ ／ 转移至 false 标签 ， 进入假分支
/ ／ 真分支 ： 仅当 0 时执行
/ ／ 执行完真分支后 ， 跳过假分支 ， 转移至结束点
/ ／ 假分支入口标签
/ ／ 假分支 ： 仅当 t= 0 时执行
/ / 整个 if-else 结构的结束点

## PDF 第 189 页

提取方式：ocr-unverified

第 4 章指令系统
下面以一个具体的 C 语言函数为例 ：
int get_cont （ n 乜 *p 1 ， int 女 p2 ） {
f (p1>p2)
return p2 ；
return *pl;
己知 pl 和 p2 对应的实参己被压入调用函数的栈帧 ， 它们对应的存储地址分别为 RIebp]+ 8 、
R[ebp]+12 （ EBP 指向当前栈帧底部 ） ， 函数返回值需存放在 eax 中 。 对应的汇编代码为
mov
mov
cmp
j be
煎 OV
〕 mp
． LI ：
mov
· L2 ：
eax, dword
Pt r
edx ， dword pt r
eaX'
.LI
eaX' dWOrd
Pt r
． L2
dWOrd
pt r
[ ebP+8 ]
[ebp+12]
[edx]
[eax]
#R[eax] [ R Cebp]+8) ， 即加载参数 pl 到 eax
#R[edx] eM[R[ebp]+12] ， 即加载参数 p2 到 e dx
# 比较 pl 和 p2 ， 根据结果设置条件码
# 若 pl 仁 p2 ， 则转移到 LI
# R [ ea 幻 —M [ R [ e dx ] ] ， 即取 p2 作为返回值
# 无条件转移到 L2 ， 跳过 else 分支
#R[eaxl [ R [ e ax ] ] ， 即取女 pl 作为返回值
pl 和 p2 是指针型参数 ， 在 32 位机器中占 4 字节 （ 一个 dword) ， 比较指令 cmp 的两个操作
数都应来自寄存器 ， 因此需先将 p ] 和 p2 对应的实参从栈中加载到寄存器 。 指针比较在 C 语言中按
无符号整数处理 ， 故使用 jbe （ 无符号 “ 小于等于 ” ） 进行条件转移 。
4 ． 3 ． 3 循环语句的机器级表示
》 《 循环语句的机器级代码分析 （ 2014 、 2017 、 2019 、 2023 冫
考点
常见的循坏结构语句有 whlle 、 for 和 do-whlleo x86 指令集中没有直接对应这些高级循环结
构的指令 ， 编译器通过条件测试 （ 如 cmp 、 test) 和条件转移 （ 如 je 、 jne 、 」 g 、 Jle 等 ） 指令的组
合来实现循环逻辑 。 实际上 ， 大多数编译器会将这三种循环统一转换为类似于 do-whlle 形式 。
（ l) do-while 循环
do-whlle 语句是一种 “ 后测试 ” 型循环 ， 其通用形式如下 ：
do
body_statement
while (test expr ） ；
/ / 先执行循环体 ， 再判断循环条件
//body statement 为循环体执行语句
//test_expr 为循环继续的条件表达式
这种结构可以直接翻译成如下所示的条件和 goto 语句组合 ：
100p ：
body_statement
t=test_expr;
f （ t ）
goto 100p ；
/ / 循环入凵标签
/ / 执行循环体语句 （ 首次进入时必然执行 ）
/ / 计算循环条件
/ 丿若条件为真 (t*0)
/ / 跳回 loop 标签 ， 继续下一轮迭代
由于循环体在条件判断之前执行 ， 因此无论 test_expr 的初始值是什么 ， body_statement 至少
会被执行一次 。 每次执行结束时 ， 程序会重新计算的值 ； 如果结果为真 （ 非零 ） ， 就跳
回循环开头继续执行 ： 否则 ， 顺序执行后续代码 ， 退出循环 。
（ 2 ） while 循环
whlle 语句是一种 “ 前测试 ” 型循环 ， 其通用形式如下 ：
while (test expr ）
body_statement
//test_expr 为循环判断的条件表达式
/ / 当 test_expr 为真时重复执行 body statement 语句
与 do-whlle 不同 ， while 循环在第一次执行 body_statement 之前就会测试 test_expr 的值 。 如
果初始条件为假 （ test_expr = 0 ） ， 那么循环体一次都不会执行 。
为了用转移指令实现这一语义 ， 编译器通常采用 “ 先判断 、 再进入循环体 ” 的策略 。 具体来

## PDF 第 190 页

提取方式：ocr-unverified

178
2027 年计算机组成原理考研复习指导
说 ， 可以将 while 循环转换为一个带前置判断的 do-while 式结构 ， 如下所示 ：
t=te st_expr ；
if 0 到
gOtO done ；
bOdy statement
while(test expr) ；
done ：
进一步展开为纯 goto 语句的形式 ：
t=te st_expr ；
if （ ！ 到
gOtO done ；
100p ：
body_statement
t=te St expr ；
if (t)
goto 100p ；
/ / 计算初始循环条件
/ / 若初始条件为假 (t=O)
/ / 跳过循环体 ， 直接转至结束点
/ / 执行循环体
/ / 重新测试条件
/ / 循环结构的结束点
/ / 计算初始循环条件
/ / 若为假
/ / 跳过循环
/ / 执行循环体
/ / 重新计算条件
/ / 若仍为真
/ / 继续下一轮
/ / 循环结束
这种转换确保了 whlle 循环 “ 先判断 、 后执行 ” 的语义 ， 同时复用了 do-Mh11e 的结构 。
（ 3 ） for 循环
for 循环是一种语法更紧凑的循环形式 ， 其通用形式如下 ：
for （ ni t_expr ； test expr ； update_expr ）
bOdy statement
//body statement 为循环体执行语句
mt_expr 为初始化表达式 ， 用于初始化循环计数器 （ 如 i 一 0 ） ； test_expr 为条件判断表达式 ， 每
次执行循环体前测试 （ 如 < 10 ） ； update_expr 为更新表达式 ， 每次循环体执行完后执行 （ 如 ． + + ） 。
从执行逻辑上看 ， for 循环完全等价于下面这段 while 循环代码
while(test expr) {
bOdy statement
update expr ，
/ / 初始化 （ 只执行一次 ）
/ / 每次循环前判断条件
/ / 执行循环体
/ / 更新循环变量
因此 ， 可以先将循环转换为 whlle 形式 ， 再按照前述方法转换为 goto 语句 ：
init expr@
t=test_expr;
if 0 到
gOtO done
100p ：
body_statement
update expr ；
t=test expr ；
if (t)
goto 100p ，
done ：
/ / 执行初始化
/ / 测试初始条件
/ / 若初始条件为假
/ / 跳过循环
/ / 执行循环体
/ / 执行史新操作
/ / 重新测试条件
/ / 若条件仍为真
/ / 跳回循环体
下面以一个具体的 C 语言函数为例 ， 展示 for 循环如何被转换为汇编代码 ．
int nsum for (int n) {
int result
result + = ；
return result;

## PDF 第 191 页

提取方式：ocr-unverified

第 4 章指令系统
在这段代码中 ， for 循环的各个组成部分如下 ：
179
init expr
test expr
update_expr
bOdy S tatement
result + = 主
通过替换前面给出的模板中的相应位置 ， 很容易将 for 循环转换为 whlle 或 do-whlle 循环 。
将这个函数翻译为 goto 语句代码后 ， 不难得出其过程体的汇编代码 ：
mov
mov
mOV
cmp
add
add
cmp
j 1e
． L2 ：
ecx ， dword pt r [ ebp+8 ]
0
edx ， 1
， CX
edX
edx ， 1
， C X
.LI
#R[ecxl—M[R[ebp]+8] ， 即加载参数 n 到 ecx
#R [ ea x ] “ 0 ， 即初始化 resu1t=0
# R [ edx ] -1 ， 即初始化 = 1
# 比较土与 n (R[edx] 与 R[ecx] ） 并设置标志位
# I f greater, 则转移到 L2
# 循环体入口
# R [ ea x ] [ e a 幻 + R [ edx ] ， 即 result + = 主
#R [ edx ] eR [ e dx ] + 1 ， 即 + +
# 再次比较 n
# I 上 less 0 r equal ， 则转移到 LI
# 循环结束点
己知实参 n 己被压入调用函数的栈帧 ， 其对应的存储地址为 RIebp] + 8 。 编译器将频繁使用的局
部变量优化到寄存器中 ： 此处 ， result 被分配到 eax （ 同时也是返回值寄存器 ） ， i 被分配到 edxo 由
于 i 和 n 均为 lnt 型 ， 判断条件 •-<n 属于有符号比较 ， 因此使用虍和 Jg 等有符号转移指令 。
4 ． 3 ． 4 过程调用的机器级表示
在程序执行过程中 ， 当一个过程 （ 函数 ） 调用另一个过程时 ， 需要完成参数传递 、 控制转移 、
现场保护与返回等一系列操作 。 x86 架构通过 call 和 ret 指令支持这一机制 ， 它们都属于无条件转
移指令 ， 但配合栈的使用 ， 实现了完整的 “ 调用 ． 返回 ” 语义 。
假定过程 p （ 调用者 ） 调用过程 Q （ 被调用者 ） ， 整个调用过程的执行步骤如下 ：
I) P 将入口参数 （ 实参 ） 放到 Q 能访问的位置 。
2 ） P 执行 call 指令 ， 将返回地址 （ call 的下一条指令地址 ） 压入栈中 ， 并转移到 QO
3 ） Q 建立自己的栈帧 ， 为局部变量分配空间 ， 并在必要时保存某些寄存器 。
4 ） Q 执行其主体代码 。
5 ） Q 将返回结果放到约定位置 ， 释放局部变量空间 ， 并恢复之前保存的寄存器 。
6 ） Q 执行 ret 指令 ， 从栈顶弹出返回地址 ， 并转移回 P 继续执行 。
上述过程中 ， 入口参数 、 返回地址 、 局部变量 、 返回结果以及需保护的寄存器内容 ， 都需要
在内存中找到合适的存放位置 。 由于寄存器数量有限 ， 且调用双方共享寄存器资源 ， 若不加保护
直接覆盖 ， 将导致程序出错 。 因此 ， 调用约定和栈的配合至关重要 。 规定 ： 寄存器 EAX 、 ECX
和 EDX 是调用者保存寄存器 ： 若 P 希望在调用后仍使用这些寄存器的值 ， 则 P 必须在调用前自
行保存 （ 如压栈 ） ， 并在返回后恢复 。 寄存器 EBX 、 FSI 、 EDI 是被调用者保存寄存器 ： 若 Q 需
要使用这些寄存器 ， 则 Q 必须在使用前将其保存到栈中 ， 并在返回前恢复 。
每个过程在执行时 ， 都会在运行时栈中分配一块专属区域 ， 称为栈帧 。 整个运行时栈由多个
连续的栈帧组成 。 EBP （ 基址指针 ） 指向当前栈帧的基址 （ 保存旧 EBP 的位置 ） ， ESP （ 栈指针 ）
始终指向当前栈顶 。 栈从高地址向低地址增长 ： 过程执行时 ， ESP 会随数据入栈 / 出栈动态变化 ，
而 EBP 在当前过程执行期间保持不变 ， 便于通过固定偏移访问参数和局部变量 。
下面用一个简单的 C 语言程序来说明过程调用的机器级实现 。
int add （ int x, int y) {
X+y;

## PDF 第 192 页

提取方式：ocr-unverified

180
int caller() {
int temp1=125;
int temp2=80 ；
int sum=add(temp1,temp2) ，
Sum;
经 GCC 编译后 ， caller 对应的汇编代码如下 ：
[ebp—12]
2027 年计算机组成原理考研复习指导
ca11er ：
push ebp
# 保存调用者 p 的 EBP
# 建立新栈帧 ： EBP - 当前栈顶
# 为局部变量和参数区分配 24 字节空间
#M[R[ebp 卜 ] 2 ] -125 ， 即 templ=125
# M 〔 R [ ebp 卜 8 ] _ 80 ， 即 temp2—80
#R[eax]—M[R[ebp 卜 8] ， 加载 temp2
#MIR[espl+4F—R[eaxl ， 将 temp2 放入参数区高地址
#R[eax]—M[R[ebp 卜 12 ] ， 加载 templ
#MIR[espll•—R[eaxl ， 将 templ 放入参数区高地址
# 调用 add ， 返回值保存于 eax
#M[R[ebpl-41•—R[eaxI ， 将返回值存入 sum
#R[eax]—M[R[ebp]-4] ， 将 sum 作为返回值
# 等价于 mov esp,ebp 和 pop ebp
# 弹出返回地址并跳回
假设 caller 被过程调用 。 在执行完 "sub esp, 24 ” 后 ， caller 的栈帧己建立 （ 见图 4 彐 2 ）
sub
mo V
mo V
ca11
ret
ebp ， e sp
esp ， 2 4
[ebp—12] ， 12 5
[ebp—8] ， 80
eax ， dword ptr
[esp+4] ,eax
eax ， dword ptr
[esp] ,eax
add
[ebp—41 ,eax
eax ， dword ptr
[ ebp
[ ebp
ESP 指向新栈顶 。 GCC 为 caller 分配了 24 字节的空间 。 从代码可见 ：
caller 仅使用了 EAX （ 属于调用者保存寄存器 ） ， 没有使用任何被调用者保存寄存器 。 因
此其栈帧中除了通过 "push ebp ” 保存调用者 P 的旧 EBP 外 ， 无须保存其他寄存器 。
caller 的三个局部变量 templ 、 temp2 和 sum 皆被分配在栈帧中 ， 占字节 。
在 call 指令调用 add 之前 ， caller 依次将入口参数 temp2 和 temp 1 的值 （ 80 和 125 ） 保存
到栈中 （ 左参数位于较低地址 ， 右参数位于较高地址 ） ， 占 8 字节 。
． 执行 call 指令时再把返回地址压入栈中 ， 占 4 字节 。
包括最初压入栈中的旧 EBP 值 （ 4 字节 ） 在内 ， caller
栈帧实际使用的空间是 4 + ] 2 + 8 + 4 一 28 字节 。 然而 ， 由
于 GCC 为了保证数据的严格对齐 ， 规定每个函数的栈帧大
小必须是 16 字节的倍数 ， 最终分配的栈帧大小为 32 字节 ，
这意味着有 4 字节 （ 未使用 ） 作为对齐填充 。
call 指令执行后 ， add 函数的返回值存放在 EAX 中 。
因此 ， call 指令后面的两条指令分别完成 ： 指令 "mov
[ebp-4] ， eax ” 将 add 的结果存入变量 sum 的存储位置 ， 该
变量位于地址 R[ebp]-4 ： 指令 “ mov eax ， dword Ptr
[ebp-4] ” 再将 sum 的值加载到 EAX ， 作为 caller 的返回值 。
在执行 ret 指令之前 ， 必须释放当前栈帧并恢复调用
者 (P) 的基址指针 。 上述第 14 行 leave 指令正是用于完
成这一任务 ， 其功能等价于以下两条指令 ．
高地址
EBP
ESP
EBP
低地址
上 BP 在 P 中的旧值
temp2 80
templ 05
未使用
125
返回地址
EBP 在 caller 中的值
曰厩的栈帧
add 栈帧底部
图 4 、 12 caller 和 add 的栈帧
mo V eSP' ebp
POP ebp
# 将栈指针 ESP 设置为当前 EBP 的值 ， 即指向保存旧 EBP 的位置
# 从栈中弹出旧 EBP 值 ， 恢复过程 P 的基址指针
执行完这两条指令后 ， EBP 己恢复为过程 P 中的原始值 ， 而 ESP 则指向栈顶的返回地址 。
此时 ， ret 指令便可从 ESP 所指位置取出该返回地址 ， 并转移回过程 P 继续执行 。 当然 ， 编译器

