# 27王道《计算机组成原理》高清带书签

原文件：[27王道《计算机组成原理》高清带书签.pdf](../../materials/27%E7%8E%8B%E9%81%93%E3%80%8A%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BB%84%E6%88%90%E5%8E%9F%E7%90%86%E3%80%8B%E9%AB%98%E6%B8%85%E5%B8%A6%E4%B9%A6%E7%AD%BE.pdf)

以下为机器提取，未人工校对；公式、上下标、图表、选项和答案必须对照原页。PDF 页码从 1 开始，不等于印刷页码。

## PDF 第 337 页

提取方式：ocr-unverified

第 7 章输入 / 输出系统
大小为 5000B ， 则 ] 秒内需产生的 DMA 次数为 5MB 巧 000B = 1000 。
CPU 用于 DMA 处理的总开销为 ] 0 巧 00 = 500000 = 0 5M 个时钟周期 。
CPU 用于外设 I/O 的时间占整个 CPU 时间的百分比为 0 、 5M 巧 00M = 旧 ％ 。
0 & 〖 解答 〗
325
本题涉及多个考点 ： 计算机的性能指标 、 存储器的性能指标 、 DMA 的性能分析 、 DMA 方式
的特点及多体交叉存储器的性能分析 。
I) 平均每秒 CPU 执行的指令数为 80M / 4 = 20M ， 因此 MIPS 数为 20 。 平均每条指令访存 ] 5
次 ， 因此平均每秒 Cache 缺失的次数一 20M × 巧 × 0 一 99 ％ ） = 300K 。 当 Cache 缺失时 ，
CPU 访问主存 ， 主存与 Cache 之间以块为传送单位 ， 此时主存带宽为 16B × 300k / s = 4.8MB/so
在不考虑 DMA 传送的情况下 ， 主存带宽至少达到 48MB/s 才能满足 CPU 的访存要求 。
2 ） 题中假定在 Cache 缺失的情况下访问主存 ， 平均每秒产生缺页中断 300000 又 0 05 ％ = 巧
次 。 因为存储器总线宽度为 32 位 ， 所以每传送 32 位数据 ， 磁盘控制器发出一次 DMA
请求 ， 因此平均每秒磁盘 DMA 请求的次数至少为 1 5 x4KB/4B = 巧 K= 巧 36 。
3 ） CPU 和 DMA 控制器同时要求使用存储器总线时 ， DMA 请求的优先级更高 。 因为 ， 若
DMA 请求得不到及时响应 ， I ／ 0 传输数据可能丢失 。
4 ） 4 体交叉存储模式能提供的最大带宽为 4 × 4B 巧 0ns = 320MB/so
09 ． 〖 解答 〗
I) 每传送一个 ASCII 码字符 ， 需要传输的位数有 1 位起始位 、 7 位数据位 (ASCII 码字符占
7 位 ） 、 ] 位奇校验位和 1 位停止位 ， 因此总位数为 1 + 7 + 1 + 1 = 10 。
I ／ 0 端口每秒最多可接收 1000 / 巧 = 20m 个字符 。
2 ） 一个字符传送时间包括 ： 设备 D 将字符送 ] / 0 端凵的时间 、 中断响应时间和中断服务程
序前巧条指令的执行时间 。 时钟周期为 ] -50MHz = 20ns ， 设备 D 将字符送 1 / 0 端口的时
间为 0 巧 ms/2 s = 巧 04 个时钟周期 。 一个字符的传送时间约为 2 5 × 104 + ] 0 + 巧 × 4 = 25070
个时钟周期 。 完成 ] 000 个字符传送所需的时间约为 10 開 × 25070 = 25070000 个时钟周期 。
CPU 用于该任务的时间约为 1 0 × 00 + 20 × 4 ） = 9 × 104 个时钟周期 。
在中断响应阶段 ， CPU 主要进行以下操作 ： 关中断 、 保存断点和程序状态 、 识别中断源 。
10 ． 〖 解答 〗
] ） 程序定时向缓存端口查询数据 ， 由于缓存端凵大小有限 ， 必须在传输完端凵大小的数据时
访问端口 ， 以防止部分数据未被及时读取而丢失 。 设备 A 准备 32 位数据所用的时间为
4B ／ 2MB ： 2 斡 ， 所以最多每隔 2 斡必须查询一次 ， 每秒的查询次数至少是 2 斡 ： 5 × 105 ，
每秒 CPU 用于设备 A 输入 ／ 输出的时间至少为 5 × 105 × 10 × 4 ： 2 × 107 个时钟周期 ， 占整个 CPU
时间的百分比至少是 2 × 107 巧 00M = 4 ％ 。
2 ） 中断响应和中断处理的时间为 400 × 0 巧佣 M ） ： 0 8 斡 ， 这时只需判断设备 B 准备 32 位数
据要多久 ， 若准备数据的时间小于中断响应和中断处理的时间 ， 则数据被刷新 ， 造成丢
失 。 经过计算 ， 设各 B 准各 32 位数据所用的时间为 4B / 40MB ： 旧斡 ， 因此设各 B 不适
合采用中断 I / 0 方式 。
3 ） 在 DMA 方式中 ， 只有预处理和后处理需要 CPU 处理 。 设各 B 每秒的 DMA 次数最多
为 40MB/s+ 1000B 一 40000 ， CPU 用于设备 B 输入 / 输出的时间最多为 40000 巧 00 一 2 × 107
个时钟周期 ， 占 CPU 总时间的百分比最多为 （ 2 × 107 ） 巧 00M ： 4 ％ 。
11. 〖 解析 〗
] ） 3 个字段的名称为柱面号 （ 或磁道号 ） 、 磁头号 （ 或盘面号 ） 、 扇区号 。 每个盘面有 20000
个磁道 ， 因此该磁盘共有 2 00 个柱面 ， 柱面号字段至少占卩 og220000 ] ： 巧位 ： 该磁盘

## PDF 第 338 页

提取方式：ocr-unverified

326
2027 年计算机组成原理考研复习指导
共有 4 个盘片 ， 每个盘片有 2 个盘面 ， 因此磁头号字段至少占 log2 （ 4 又 2 ） ： 3 位 ： 每个磁道
有 500 个扇区 ， 因此扇区号字段至少占卩 og2500 ] ： 9 位 。
2 ） 一个扇区的访问时间由寻道时间 、 延迟时间 、 传输时间三部分组成 。 平均寻道时间为 5ms ，
平均延迟时间等于磁盘转半圈所需要的时间 ， 平均传输时间等于一个扇区划过磁头下方
所需要的时间 。 而该磁盘转一圈的时间为 （ 60 × ] 03V7200 8 33ms ， 因此一个扇区的平均访
问时间约为 5 + 8 33 ／ 2 + & 33 巧 00 9 ． 18ms 。
3 ） 磁盘控制器中的数据缓冲区每充满一次 ， DMA 控制器就需要发出一次总线请求 ， 将这
64bit 数据通过总线传送到主存 ， 因此 ， 在一个扇区的读 / 写过程中 ， DMA 控制器向 CPU
发送了 512B/64bIt = 64 次总线请求 。 采用周期挪用 DMA 方式 ， 因此当 CPU 和 DMA 控
制器都需要访问主存时 ， DMA 控制器可以优先获得总线使用权 。 因为一旦磁盘开始读 ／
写 ， 就必须按时完成数据传送 ， 否则数据缓冲区中的数据会发生丢失 。
7 ， 4 本章小结
本章开头提出的问题的参考答案如下 。
0 ] / 0 设备有哪些编址方式 ？ 各有何特点 ？
I/O 设各的编址方式主要有统一编址和独立编址 。 统一编址是指将主存地址空间的一部分分
配给 ] / 0 端口 ， CPU 可使用普通访存指令直接访问 ] / 0 设各 ， 编程間便 ： 但会占用主存地址空间 ，
减少可用主存容量 。 独立编址是指 I/O 地址空间与主存地址空间相互独立 ， I/O 端口不占用主存资
源 ； 但访问 I/O 设备需使用专用 I/O 指令 （ 如 IN 、 OUT) ， 增加了指令集的复杂性 。
2 ） CPU 响应中断应具备哪些条件 ？
． CPU 内部的中断屏蔽触发器处于开放状态 。
@ 外设发出中断请求 ， 且其接口中的中断请求触发器置为 “ 1 ” ， 以维持中断请求信号 。
0 外设接口的中断允许触发器为 “ 1 ” ， 才能将中断请求送达 CPUO
当上述三个条件同时满足时 ， CPU 在当前指令执行结束后响应中断 。
7 · 5 常见问题和易混淆知识点
l. 在开中断情况下 ， CPU 检测到中断就一定会立即响应吗 ？
是的 。 在开中断状态下 ， CPU 总在每条指令执行结束时采样中断请求 。 一旦检测到中断请求 ，
即表明该请求未被屏蔽且有效 ， CPU 必定会立即响应 。
需要说明的是 ： 在单重中断系统中 ， 进入中断服务程序后通常会自动关中断 ， 此后即使有新
的中断请求也无法被检测 ， 但这不属于 “ 开中断 ” 情形 ， 违背题设前提 。 而在多重中断系统中 ，
高优先级中断可在低优先级的中断服务程序执行期间被检测并响应 ， 因其未被屏蔽 。
2 ． 向量中断 、 中断向量 、 向量地址三个概念是什么关系 ？
中断向量 ： 中断向量指中断服务程序的入口地址 。 每个中断源对应一个唯一的中断服务程序 ，
其入口地址即为该中断源的中断向量 。 系统通常将所有中断向量集中组织成一张中断向量表 ： 也
有系统在表中存放转移指令 ， 构成中断向量转移表 。
向量地址 ： 向量地址指中断向量表 （ 或转移表 ） 中某一项的内存起始地址 。 它通常由中断类

## PDF 第 339 页

提取方式：ocr-unverified

型号 （ 也称中断号 ） 乘以表项长度计算得出 。 例如 ， 若表项占 4 字节 ， 中断类型号为 5 ， 则向量
地址为 5 × 4 = 20 （ 假设表从 0 开始 ） 。 因此 ， 向量地址是 CPU 用于定位中断向量的 “ 索引位置 ”
向量中断 ： 是一种能够自动识别中断源并快速转移至对应服务程序的中断处理方式 。 当中断
发生时 ， 中断源 （ 或中断控制器 ） 向 CPU 提供中断类型号 ， CPU 据此生成向量地址 ， 并从向量
表中取出对应的中断向量 （ 入口地址 ） ， 直接转移执行相应的中断服务程序 。
3 ． 程序中断和调用子程序有何区别 ？
虽然程序中断和子程序调用都会引起程序执行流程的转移 ， 但二者在本质上存在显著差异 ，
主要体现在以下几个方面 ：
D 触发时机 ： 子程序调用由程序员通过 CALL 指令主动发起 ， 发生时刻和位置是确定的 ：
而中断由外部设备或内部事件随机触发 ， CPU 只在每条指令执行结束时采样中断请求 ，
一旦条件满足便转入中断处理 ， 具有明显的异步性和不可预测性 。
2 ） 服务关系 ： 子程序完全为主程序服务 ， 二者构成主从关系 ： 主程序在需要时调用子程序 ，
执行完毕后返回主程序继续运行 。 中断服务程序通常与主程序无直接关联 ， 是为响应外
部事件 （ 如数据到达 、 定时器溢出等 ） 而执行的独立任务 ， 二者属于平行关系 。
3 ） 实现机制 ： 子程序调用是纯软件行为 ， 只需利用堆栈保存返回地址 ， 无须额外硬件支持 ；
而中断处理依赖软硬件协同 ， 需借助中断请求线 、 中断控制器和优先级判优电路等硬件 ，
才能完成中断请求的接收 、 判优 、 响应和断点保护等操作 。
4 ） 嵌套级别 ： 子程序可多层嵌套 ， 嵌套深度主要受限于系统堆栈空间 ； 中断嵌套则受优先
级机制约束一一只有更高优先级的中断才能打断当前正在执行的中断服务程序 。 出于硬
件复杂性和实时性考虑 ， 实际系统中中断优先级通常较少 ， 嵌套层次有限 。
购买王道书 ， 就上
王道官方考研书店
wangdao.taobao ℃ om
第 7 章输入 / 输出系统
0
327

## PDF 第 340 页

提取方式：ocr-unverified

参考文献
袁春风 ， 计算机系统基础 [ M ] ， 北京 ： 机械工业出版社 ， 201 &
卩 ]
袁春风 ． 计算机组成与系统结构 [ M ] ， 北京 ： 清华大学出版社 ， 20 巧 ．
卩 ]
袁春风 ． 计算机系统基础习题解答与教学指导 [ M ] ． 北京 ： 机械工业出版社 ， 2019 ．
卩 ]
唐朔飞 ． 计算机组成原理 [ M ] ． 北京 ： 高等教育出版社 ， 2008 ．
[ 4 ]
唐朔飞 ． 计算机组成原理 ： 学习指导与习题解答 [ M ] ． 北京 ： 高等教育出版社 ， 2012 ．
巧 ]
布莱恩特 ， 奥哈洛伦 ， 等 ． 深入理解计算机系统 [ M ] ． 3 版 、 龚奕利 ， 贺莲 ， 译 ． 北京 ： 机械工业出版
[ 6 ]
社 ， 2016 ，
李春葆 ， 肖忠付 ， 杭小庆 ． 计算机组成原理联考辅导教程 [ M ] ． 北京 ： 清华大学出版社 ， 2m0 ．
囝
全国考研计算机大纲配套教材专家委员会 ． 全国硕士研宄生入学统一考试计算机专业基础综合考试大
罔
纲解析 [ M ] ． 北京 ： 高等教育出版社 ， 2014 ．
徐爱萍 ． 计算机组成原理考翡指导 [ M ] ． 北京 ： 清华大学出版社 ， 2003 ．
[ 9 ]
卩 0 ] 谭志虎 ， 计算机组成原理 （ 微课版 ） [M] ， 北京 ： 人民邮电出版社 ， 2022 ，
购买王道书 ， 就上
王道官方考研书店
wangdao.taobao ℃ om
0

